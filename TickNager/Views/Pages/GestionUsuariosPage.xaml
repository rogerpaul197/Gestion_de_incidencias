<!-- GestionUsuariosPage.xaml -->
<Page x:Class="TickNager.Views.Pages.GestionUsuariosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">
    <!--
    <Grid Background="#0C0D21" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Border CornerRadius="10"
                    Background="#171A45"
                    Height="30"
                    Width="120"
                    Margin="0,0,20,0">
                <Button Content="Añadir usuario"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"
                        Command="{Binding AnadirUsuarioCommand}"/>
            </Border>

            <Border CornerRadius="10"
                    Background="#171A45"
                    Height="30"
                    Width="120">
                <Button Content="Eliminar usuario"
                        Background="Transparent"
                        Foreground="White"
                        BorderThickness="0"/>
            </Border>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Margin="0,10,0,0">
            <ItemsControl ItemsSource="{Binding Usuarios}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#171A45"
                                CornerRadius="8"
                                Margin="0,0,0,10"
                                Padding="10">
                            <StackPanel>
                                <TextBlock Foreground="White"
                                           FontSize="16"
                                           FontWeight="Bold">
                                    <Run Text="{Binding nombreUsuario}"/>
                                    <Run Text=" "/>
                                    <Run Text="{Binding apellidoUsuario}"/>
                                </TextBlock>

                                <TextBlock Text="{Binding departamentoUsuario}"
                                           Foreground="White"
                                           Margin="0,5,0,0"/>

                                <TextBlock Text="{Binding correoUsuario}"
                                           Foreground="White"
                                           Margin="0,2,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>-->
</Page>